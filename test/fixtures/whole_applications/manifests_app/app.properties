# Application properties
app.name=MyApplication
app.version=1.0.0
# Multi-line property with continuation
app.long.description=This is a very long property value that \
  continues on the next line with additional configuration details
server.port=9000
server.ssl=true
# Property with interpolation reference
db.url=%(db.host)s:%(db.port)s
db.host=localhost
db.port=5432
